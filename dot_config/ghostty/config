theme = dark:catppuccin-mocha,light:catppuccin-mocha
macos-titlebar-style = hidden

window-padding-x = 8,8
window-padding-y = 12,5

font-family = "Monaspace Neon Var"
font-size = 14

# Enable all ligature-related features
font-feature = liga 1  # Standard ligatures
font-feature = calt 1  # Contextual alternates (programming ligatures)
font-feature = dlig 1  # Discretionary ligatures
font-feature = hlig 1  # Historical ligatures
font-feature = clig 1  # Contextual ligatures

# Enable stylistic sets (font-specific, may contain additional ligatures)
font-feature = ss01  1
font-feature = ss02  1
font-feature = ss03  1
font-feature = ss04  1
font-feature = ss05  1
font-feature = ss06  1
font-feature = ss07  1
font-feature = ss08  1
font-feature = ss09  1
font-feature = ss010 1
font-feature = zero 1  # Slashed zero
font-family = Symbols Nerd Font

### Needed for commit mono
# adjust-cell-height = 1
# adjust-cursor-height = 1
adjust-underline-position = 4
